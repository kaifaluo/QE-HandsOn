clear all; clc;

rawPos = load('output.txt');

numLoop = 12;

posCl1 = rawPos(1:numLoop);
posCl2 = rawPos(numLoop+1:2*numLoop);
totalEnergy = rawPos(2*numLoop+1:3*numLoop);

clf; fig = figure(1);
yyaxis left
scatter(1:numLoop,(posCl2-posCl1)*0.529,12); hold on
plot(1:numLoop,(posCl2-posCl1)*0.529)
ylabel('Cl-Cl Distance (\AA)')
plot(

yyaxis right
scatter(1:numLoop,totalEnergy); hold on
plot(1:numLoop,totalEnergy)
ylabel('Total Energy (Ry)')

xlim([1,numLoop])
set(gca,'box','on')